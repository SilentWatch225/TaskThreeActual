import React from 'react';
import { TouchableHighlight, Text, StyleSheet } from 'react-native';

function FirstComp ({title, onPress, buttonColor, textColor, borderColor}){
    return(
        <TouchableHighlight
        onPress = {onPress},
        buttonColor


        </TouchableHighlight>
    );
}